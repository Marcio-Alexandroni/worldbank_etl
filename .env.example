# ── Configuração do banco PostgreSQL ──
POSTGRES_USER=etl_user
POSTGRES_PASSWORD=etl_pass
POSTGRES_DB=worldbank
POSTGRES_HOST=db
POSTGRES_PORT=5432

# ── URL de conexão (montada automaticamente pelo config.py) ──
# DATABASE_URL=postgresql://etl_user:etl_pass@db:5432/worldbank
